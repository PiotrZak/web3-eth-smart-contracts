'use strict';

// CREATE TABLE prod.vci_wallet (
// 	id serial NOT NULL,
// 	uid varchar(250) NOT NULL,
// 	"action" varchar(250) NOT NULL,
// 	wallet varchar(250) NOT NULL,
// 	created timestamptz NOT NULL DEFAULT now()
// );

module.exports = function(Wallet) {

};
